name: Test and release

on:
  workflow_dispatch:
  push:
    paths-ignore:
      - '**.md'
    branches:
      - "master"

jobs:
  shared:
    uses: Shanridhu/github-action-maven-example-start/.github/workflows/main.yml@v3
    with:
      enable-code-scans: true
    secrets:
      ghcr-token: ${{ secrets.shared_secret }} 
